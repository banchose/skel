## Dynamic configuration
http:
  routers:
    my-router2:
      rule: "Path(`/foo2`)"
      service: ealen
      entrypoints:
        - web
  services:
    ealen:
      loadBalancer:
        servers:
          - url: "http://echo-server:80"
