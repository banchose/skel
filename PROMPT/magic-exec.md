---
name: magic-exec
description: Output a command that is runable in bash as if and nothing more
---

# Output a bash runable string

Output only the shell command to run. No explanation, no markdown, no code fences. Just the raw command.


