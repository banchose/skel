aws sts get-caller-identity  --region us-east-1 --profile test
aws sso login --profile test --use-device-code --no-browser
echo "${AWS_BEARER_TOKEN_BEDROCK}"
cd ~/.bashrc_zen.d
cd ~/gitdir/skel
cd ~/gitdir/skel/docker/builds/template
cd ~/temp
v ~/.bashrc
cd ~/work
cd ~/work/scripts

