FROM python:3.11-slim
WORKDIR /app
RUN pip install uv
# Replace with your MCP server command; example: uvx mcp-server-time
CMD ["uvx", "--with", "mcp<2", "mcpo", "--host", "0.0.0.0", "--port", "8000", "--", "uvx", "mcp-server-time", "--local-timezone=America/New_York"]
